import TitleBar from "../../components/TitleBar/TitleBar";

const WebDev = () => {
    return (
        <div className="main-background">
            <TitleBar />
        </div>
    );
};

export default WebDev;